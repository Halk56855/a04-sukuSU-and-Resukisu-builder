name: Build Samsung A04 Kernel with ReSukiSU

on:
  workflow_dispatch: # يتيح تشغيل البناء يدوياً من تبويب Actions في قيت هب

jobs:
  build:
    runs-on: ubuntu-22.04 # بيئة لينكس الافتراضية للبناء

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libncurses-dev bison flex libssl-dev bc git python3 wget zip

      - name: Setup Toolchain (GCC/Clang)
        run: |
          git clone --depth=1 https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 -b android-11.0.0_r30 toolchain

      - name: Inject ReSukiSU via Official Script
        run: |
          cd drivers/
          # تشغيل السكريبت الرسمي لدمج ReSukiSU تلقائياً
          curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/main/kernel/setup.sh" | bash
          cd ..

      - name: Add Manual Hooks to Defconfig
        run: |
          # إضافة إعدادات التكوين المطلوبة للخطافات اليدوية بملف الـ defconfig
          DEFCONFIG_FILE="arch/arm64/configs/mtk_defconfig" # استبدل اسم الملف حسب مسار الdefconfig الفعلي في مستودعك
          echo "CONFIG_KSU=y" >> $DEFCONFIG_FILE
          echo "CONFIG_KSU_MANUAL_HOOK=y" >> $DEFCONFIG_FILE

      - name: Compile Kernel
        run: |
          export ARCH=arm64
          export SUBARCH=arm64
          export PATH=$PWD/toolchain/bin:$PATH
          export CROSS_COMPILE=aarch64-linux-android-

          # تجهيز إعدادات النواة وبدء التجميع باستخدام كافة أنوية المعالج السحابي
          make mtk_defconfig
          make -j$(nproc)

      - name: Upload Built Kernel Image
        uses: actions/upload-artifact@v4
        with:
          name: Samsung-A04-Kernel-ReSukiSU
          path: arch/arm64/boot/Image.gz-dtb
