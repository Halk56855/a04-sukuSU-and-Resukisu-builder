name: Build SukiSU-Ultra KernelPatch for Galaxy A04m

on:
  workflow_dispatch: # للبدء اليدوي من تبويب Actions

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Maximize Build Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 16384
          temp-reserve-mb: 4096
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'

      - name: Checkout Kernel Source (rsuntk-oss/latest-B)
        uses: actions/checkout@v4
        with:
          repository: rsuntk-oss/android_kernel_samsung_a04m
          ref: latest-B

      - name: Clone SukiSU Patch Repository
        run: |
          git clone https://github.com/SukiSU-Ultra/SukiSU_patch.git sukisu_patch

      - name: Apply Kernel Source Patch (4.19)
        run: |
          cd ${{ github.workspace }}
          # تطبيق رقعة الإصدار 4.19 الخاصة بنواة A04m
          git apply sukisu_patch/4.19/ksu_hooks_sukisu_4.19-patch || git apply --whitespace=fix sukisu_patch/4.19/ksu_hooks_sukisu_4.19-patch

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential ccache curl flex g++-multilib gcc-multilib git gnupg gperf imagemagick lib32ncurses5-dev lib32z1-dev libelf-dev liblz4-tool libncurses5-dev libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc yasm zip zlib1g-dev

      - name: Download Toolchains
        run: |
          git clone --depth=1 https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9.git toolchain-gcc64 || true
          git clone --depth=1 https://github.com/LineageOS/android_prebuilts_clang_host_linux-x86_clang-r416183b.git clang-llvm || true

      - name: Configure and Compile Kernel
        run: |
          export ARCH=arm64
          export SUBARCH=arm64
          export PATH="${{ github.workspace }}/clang-llvm/bin:${{ github.workspace }}/toolchain-gcc64/bin:$PATH"
          
          # إعداد الـ defconfig الخاص بالمستودع
          make O=out mtk_defconfig || make O=out a04_defconfig || true
          
          # تفعيل المتطلب الإجباري CONFIG_KALLSYMS الخاص بـ KernelPatch
          scripts/config --file out/.config --enable CONFIG_KALLSYMS
          scripts/config --file out/.config --enable CONFIG_KALLSYMS_ALL
          
          # بناء الكيرنل مع تجاوز التحذيرات الصارمة للمترجم الحديث
          make O=out -j$(nproc) ARCH=arm64 CC=clang CLANG_TRIPLE=aarch64-linux-gnu- CROSS_COMPILE=aarch64-linux-android- KCFLAGS="-Wno-error=implicit-function-declaration -Wno-error=incompatible-pointer-types -Wno-error=int-conversion"

      - name: Apply SukiSU KernelPatch (KPM)
        run: |
          cd ${{ github.workspace }}
          # استخدام الأدوات المتوفرة داخل مجلد kpm بمستودع SukiSU_patch
          chmod +x sukisu_patch/kpm/kptools
          
          KERNEL_IMAGE="out/arch/arm64/boot/Image"
          if [ -f "out/arch/arm64/boot/Image.gz-dtb" ]; then
            KERNEL_IMAGE="out/arch/arm64/boot/Image.gz-dtb"
          elif [ -f "out/arch/arm64/boot/Image.gz" ]; then
            KERNEL_IMAGE="out/arch/arm64/boot/Image.gz"
          fi
          
          # الحقن باستخدام أداة kptools و kpimg المحلية
          sukisu_patch/kpm/kptools -p -i "$KERNEL_IMAGE" -k sukisu_patch/kpm/kpimg -o patched_image

      - name: Pack with AnyKernel3
        run: |
          git clone https://github.com/Kernel-SU/AnyKernel3 -b master anykernel
          rm -rf anykernel/.git
          
          if [ -f "patched_image" ]; then
            cp patched_image anykernel/Image
          else
            cp out/arch/arm64/boot/Image anykernel/Image
          fi
          
          cd anykernel
          zip -r ../SukiSU-Ultra-KernelPatch-A04m.zip *

      - name: Upload Built Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SukiSU-Ultra-KernelPatch-A04m
          path: SukiSU-Ultra-KernelPatch-A04m.zip
